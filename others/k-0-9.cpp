#include <iostream>
#include <vector>
using namespace std;

int walk_count(int k) {
    if (k % 2 != 0) {
        return 0;
    }

    vector<vector<int>> dp(9, vector<int>(9,0));

    for (int i = 0; i < k; ++i) {
        for (int j = 0; j < 9; ++j) {
            dp[a][j] = dp[a][]
        }
    }
}

int main(int argc, char const *argv[]) {

    return 0;
}
